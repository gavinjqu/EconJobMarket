# OS
.DS_Store

# IDE
.vscode/
.idea/

# Python
.venv/
venv/
__pycache__/
*.egg-info/
dist/
build/

# Environment
.env

.claude

# SQLite sidecar files — never commit these: a checkout that pairs the .db
# with a stale WAL can present pre-migration state or corrupt the database.
*.db-wal
*.db-shm

# Migration backups (~30MB each)
data/backups/

docs/