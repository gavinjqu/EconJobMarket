services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: amm
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: amm
    ports:
      - "5432:5432"
    volumes:
      - amm_pgdata:/var/lib/postgresql/data

volumes:
  amm_pgdata:
